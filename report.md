# 上线前优化报告

日期：2026-08-14

本轮以新存档开服为目标，完成市场、房型、界面、中文和英文文案的收尾审查。

## 已完成

### 市场

- 五个国家拥有互相独立的市场头寸和价格。
- 市场从净库存 `0` 开始，允许负库存和做市商负债。
- 买卖总价按指数价格曲线积分结算，大额交易不会错误地使用“当前单价 × 数量”。
- 买入向上取整，卖出向下取整并收取 1% 手续费；原路买卖只会亏损，不能无成本套利。
- 买入数量变化时只更新当前页面的报价按钮，不再重建整个市场界面。
- 市场仍只允许角色实际位于本国公共星球、难民营或房产时使用；远程视图不能绕过地点限制。
- 算法、公式和配置位置见 [market.md](market.md)。

### 房产与自动交易

- 红瓶房改为“出售小屋”，绿瓶房改为“购买小屋”。
- 两种房型各自生成一个常量运算器和一个钢箱。
- 玩家在运算器中选择一种可交易物品；脚本显示该物品的当前市场价。
- 屋主在线时，每分钟最多自动处理 1000 件。
- 自动交易与手动交易都不消耗体力，仍受本国区域限制。
- 红瓶、绿瓶房的面积与总寿命按安全小屋的基础规格设置；各自保留独立的降价周期 T。
- 铁路小屋和网格小屋的特殊地形以门口为基准向上展开，升级后的布局与直接建造更一致。
- 国有房产初始价格使用普通建造价格的 1%。
- 自动交易失败时不会吞掉从钢箱中部分取出的物品。
- 常量运算器显示值限制在 32 位信号上限内，极端高价不会造成接口越界。

### 界面

- 左上角只保留“操作”按钮；再次点击会关闭已经打开的主窗口。
- 删除旧传送按钮和循环前往自有房产按钮，门口区域成为公共星球、难民营与房产之间的主要交通方式。
- 个人页分为体力、金币低保、科技瓶经验三个区域。
- 体力和低保进度条均显示名称、当前值与上限，不再混淆。
- 星球与阵营合并为同一张五行表，集中显示倒计时、特色、关系、人口、房产、飞船和转籍入口。
- 飞船列表增加远程查看按钮；角色和背包留在原处。
- 管理员页增加“增加测试金币”按钮。
- 大型列表继续使用事件版本号或内容签名判断结构变化；个人页每秒更新，房产、玩家、飞船和市场只做每分钟兜底刷新。

### 文案与本地化

- 场景列表简介已从旧“点击左上角传送”改为实际的门口通行方式。
- 简介补充五国独立市场，同时保持快速入门篇幅简短。
- 中文和英文房型说明明确写出：在运算器选择货物、钢箱承担自动买卖库存。
- 其他语言同步英文回退，避免继续向玩家展示旧房型机制。
- 中英文继续承担主要文案质量；公式和细节放在 tooltip、规则数据和 `market.md`，主界面只保留操作所需信息。

## 市场算法摘要

令净库存为 `q`、初始单价为 `P0`、市场深度值为 `V`、价格倍率为 `K`：

```text
D = V / P0
p(q) = P0 × K^(-q / D)
```

玩家卖货使 `q` 增加并压低价格；玩家买货使 `q` 减少并抬高价格。

`q=0` 时单价正好是配置的 `base_price`。市场即使没有初始货物也能先向玩家供货；负库存表示做市商欠货，不要求立即清偿。

一笔交易使用曲线在交易区间内的积分，所以成交会完整计算交易自身造成的价格变化。做市商可以暂时亏损或负债，系统不维护必须为正的市场现金池。

## 验证结果

已通过：

- `luac -p control.lua config.lua config/*.lua scripts/*.lua`
- `check_scenario.sh .`
- `git diff --check`
- `python3 -m json.tool info.json`
- locale 键集合、占位符和富文本静态检查

已用官方 Factorio 2.0.72 runtime API 核对：

- `LuaConstantCombinatorControlBehavior.sections_count`
- `get_section()` / `add_section()`
- `LuaLogisticSection.get_slot()` / `set_slot()`
- `LuaEntity.minable_flag`
- `LuaPlayer.set_controller()` 的 remote controller 参数
- `LuaPlayer.physical_surface`

这些接口与当前实现一致。

## 仍需实机验证

静态检查不能替代 Factorio 引擎内测试。

开服前建议在最终客户端完成以下短测试：

1. 新建红瓶出售小屋，选择市场物品，把货物放入钢箱，等待一分钟并核对金币、库存与运算器价格。
2. 新建绿瓶购买小屋，选择市场物品，核对金币不足、钢箱已满和正常购买三条路径。
3. 两名不同阵营玩家同时打开市场，确认五国库存与价格互不串联。
4. 打开个人页，核对体力、低保和经验三个区域在常用分辨率下不挤压。
5. 检查星球与阵营合并表、房产表和飞船表的滚动与 tooltip。
6. 从飞船列表进入远程视图，再返回角色，确认角色位置与背包不变。
7. 各建造一次铁路小屋和网格小屋，并扩建一级，核对门口、关联箱、装卸机和特殊地形。
8. 使用两个客户端执行一次外国现场转籍与一次加入地球，核对房产充公、关联箱清理、飞船保留和复活时间。

## 部署说明

- 当前按新存档开服设计，不需要执行旧布局迁移命令。
- 应完整替换场景目录后创建新存档，避免脚本与 locale 版本不一致。
- 如果仅在临时测试存档重载脚本，可执行 `/c game.reload_script()`。
- 市场曲线版本为 4；从更早曲线版本加载时，五国市场头寸会重置为零，但玩家金币和背包不变。

## 本轮提交

- `d9b3db8`：零库存本地市场定价。
- `ec37245`：每阵营一艘飞船及远程查看。
- `dc6bf3c`：现场转籍与飞船保留。
- `d5202d2`：自动交易房型及市场边界修复。
- `193e517`：导航、个人页和星球阵营表重构。
- `b9bd6b1`：场景简介与房型文案同步。
