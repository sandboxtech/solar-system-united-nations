# 阶段 0 游戏内验证

这些测试只使用专用表面 `un-stage0-lab`，不会清理 Nauvis、五颗公共星球或玩家资产。

## 新存档

1. 使用 Space Age 启动本场景。
2. 以管理员身份执行 `/un-stage0 status`。
3. 确认 Space Age 和原版关联箱均显示“是”。
4. 执行 `/un-stage0 linked`。
5. 确认提示“直接访问库存和切换 link ID 均正常”。
6. 执行 `/un-stage0 clear`。
7. 等待“已收到异步完成事件”的提示。
8. 再次执行 `/un-stage0 status`，两个测试应显示 `passed`。
9. 执行 `/un-stage0 cleanup` 删除专用测试表面。

## 保存与多人

1. 保存并重新加载游戏，再执行 `/un-stage0 status`，确认测试状态仍然存在。
2. 让第二名非管理员玩家尝试执行 `/un-stage0 linked`，确认被拒绝。
3. 让第二名玩家远程查看 `un-stage0-lab`，再执行 `/un-stage0 cleanup`，确认清理被安全拒绝。
4. 离开测试表面后再次清理，确认表面进入删除队列。

## 预期日志

正常通过时不应出现 `[un] event ... failed`。若出现该日志，保留存档与 `factorio-current.log` 以便定位。
