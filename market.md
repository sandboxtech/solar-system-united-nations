# 国家市场参数模拟报告

日期：2026-08-14

## 最终选择

市场采用倒数曲线 `1/x`，不再使用指数衰减曲线。

```lua
M.market_initial_cash = 20000000
M.market_depth_value = 1000000
M.market_sell_fee_rate = 0.01
M.market_tax_share = 0.50
```

每种商品只需要配置 `base_price`。

`market_depth_value` 是唯一的商品流动性参数。

## 价格公式

令：

- `q` 为本国市场中的商品库存；
- `P0` 为商品基础价格；
- `V = 1,000,000` 为库存价值深度；
- `L` 为连续定价使用的本国市场资金；
- `V0 = 20,000,000` 为初始市场资金。

一种商品的数量深度为：

```text
D = V / P0
```

即时单价为：

```text
p(q,L) = P0 / (1+q/D) × L/V0
```

也可以令 `B=P0×q`，把 `B` 理解为按基础价计算的库存价值：

```text
p(B,L) = P0 / (1+B/V) × L/V0
```

当一种商品的净库存价值达到 `V` 时，该商品的库存价格因子正好减半。

因此，无论是 `10,000` 个基础价 100 的铁矿，还是 `100` 个基础价 10,000 的处理器，都会产生相同的库存价格影响。

## 交易积分

大额交易不能使用“当前单价乘数量”，但不需要逐件计算。

从库存价值 `B1` 走到 `B2` 的积分为：

```text
I(B1,B2) = V × ln((V+B2)/(V+B1))
```

玩家买入 `x` 件时：

```text
I = I(P0×(q-x), P0×q)
L' = L × exp(I/V0)
总价 = L' - L
```

玩家卖出 `x` 件时，设手续费率为 `f`、手续费进入市场的比例为 `s`：

```text
I = I(P0×q, P0×(q+x))
h = 1 - f×s
L' = L × exp(-hI/V0)
毛收入 = (L-L') / h
玩家所得 = 毛收入 × (1-f)
```

这仍然是常数时间的闭式解。

代码只使用一次 `ln` 和一次 `exp`，不随交易数量增加循环次数。

整数结算会累计小数余数，所以整单与拆单的最终金币相同。

## 为什么选择倒数曲线

旧指数曲线在正常工厂产量下下降得太快。

指数曲线的库存积分存在固定上限，所以无限卖出同一种商品时，该商品能换到的总金币也很快接近固定上限。

倒数曲线的积分为对数，会无限但缓慢增长。

在此前选定的指数参数与最终倒数参数之间对比：

| 单商品基础总价 | 指数曲线所得 | 指数成交后价格 | 倒数曲线所得 | 倒数成交后价格 |
|---:|---:|---:|---:|---:|
| 100,000 | 约 95,500 | 初始价的 93.1% | 约 94,100 | 初始价的 90.5% |
| 1,000,000 | 约 709,600 | 初始价的 49.5% | 约 674,500 | 初始价的 48.3% |
| 10,000,000 | 约 1,429,000 | 初始价的 0.12% | 约 2,238,000 | 初始价的 8.1% |
| 100,000,000 | 约 1,431,000 | 接近零 | 约 4,082,000 | 初始价的 0.79% |

普通交易区间的结果相近。

区别主要出现在大型工厂持续倾销以后：倒数曲线保留了更长的价格尾部。

这意味着：

- 少量供货接近基础价格；
- 大量供货仍会明显降价；
- 再多的库存也不会让单价很快接近无法交易的零价；
- 继续供货仍有价值，但边际收益越来越低；
- 全国市场资金会限制实际总支出，市场不会负债。

## 单商品卖出模拟

以下均从空库存、市场资金 `20,000,000` 开始。

“基础总价”是按初始单价直接相乘的金额，不是实际成交额。

| 基础总价 | 玩家税后所得 | 平均成交比例 | 成交后即时价格 | 剩余市场资金 |
|---:|---:|---:|---:|---:|
| 10,000 | 9,848 | 98.5% | 初始价的 99.0% | 99.95% |
| 100,000 | 94,134 | 94.1% | 初始价的 90.5% | 99.53% |
| 500,000 | 397,388 | 79.5% | 初始价的 65.3% | 98.00% |
| 1,000,000 | 674,519 | 67.5% | 初始价的 48.3% | 96.61% |
| 2,000,000 | 1,058,438 | 52.9% | 初始价的 31.6% | 94.68% |
| 5,000,000 | 1,697,079 | 33.9% | 初始价的 15.2% | 91.47% |
| 10,000,000 | 2,237,785 | 22.4% | 初始价的 8.1% | 88.75% |
| 100,000,000 | 4,082,388 | 4.1% | 初始价的 0.79% | 79.48% |

相比指数曲线，极大量供货后的价格更有用。

同时收益增长远慢于供货量，单一流水线不会按基础价格无限印钱。

## 代表性货物

| 货物 | 数量 | 基础总价 | 玩家税后所得 | 成交后单价 |
|---|---:|---:|---:|---:|
| 铁矿 | 1,000 | 100,000 | 94,134 | 约 90 |
| 铁矿 | 10,000 | 1,000,000 | 674,519 | 约 48 |
| 处理器 | 10 | 100,000 | 94,134 | 约 9,048 |
| 处理器 | 100 | 1,000,000 | 674,519 | 约 4,831 |
| 钷素科技包 | 1 | 100,000 | 94,134 | 约 90,479 |
| 钷素科技包 | 10 | 1,000,000 | 674,519 | 约 48,305 |

相同基础总价具有相同的价格冲击。

高价物品不需要单独配置市场深度。

## 全国市场压力测试

假设连续向不同商品各卖出 `1,000,000` 基础价值，而且期间无人买回：

| 商品种类数 | 剩余市场资金 | 全国价格系数 | 玩家累计所得 |
|---:|---:|---:|---:|
| 1 | 19,322,075 | 96.6% | 674,519 |
| 5 | 16,832,507 | 84.2% | 3,151,576 |
| 10 | 14,166,664 | 70.8% | 5,804,023 |
| 20 | 10,034,717 | 50.2% | 9,915,206 |
| 40 | 5,034,778 | 25.2% | 14,890,020 |
| 59 | 2,614,760 | 13.1% | 17,297,877 |

这是只卖不买的压力测试。

市场资金越少，全部商品价格和后续卖出收入都会下降。

买家购买库存会把资金送回市场，并抬高相应商品价格。

## 往返交易

同一名玩家先卖出、再立即买回完全相同的数量：

| 基础总价 | 卖出所得 | 买回成本 | 往返损失 |
|---:|---:|---:|---:|
| 100,000 | 94,134 | 95,086 | 952 |
| 1,000,000 | 674,519 | 681,392 | 6,873 |
| 5,000,000 | 1,697,079 | 1,714,612 | 17,533 |
| 10,000,000 | 2,237,785 | 2,261,080 | 23,295 |

往返损失约为卖出所得的 `1%`。

同一市场内没有无成本往返套利。

## 税款影响

默认有一半房产税、转账手续费和市场卖出手续费进入本国市场。

从初始状态计算：

| 全部税款 | 进入市场 | 全国价格提高 |
|---:|---:|---:|
| 100,000 | 50,000 | 0.25% |
| 1,000,000 | 500,000 | 2.5% |
| 5,000,000 | 2,500,000 | 12.5% |
| 10,000,000 | 5,000,000 | 25% |

税收能缓慢补充市场流动性，但少量税款不会立刻显著推高全部商品。

## 结论

倒数曲线比指数曲线更适合首轮公开测试。

它的参数含义更直观，积分仍然简单，而且对大型自动化工厂更宽容。

市场没有由公式单独决定的唯一均衡库存。

实际平衡点由玩家愿意接受的价格和真实供需决定。

开服后建议只观察三项数据：各国市场资金、主要原料库存、主要物品即时价格。

若市场经常低于初始资金的 `20%`，优先提高 `market_initial_cash`。

若单一商品价格下降太快，再提高 `market_depth_value`。
