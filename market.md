# 本地市场定价

每个国家拥有一个独立市场。

市场初始净库存为 `0`，但玩家仍然可以买入货物。市场库存允许成为负数；负库存表示做市商暂时欠货，不是代码错误。做市商也没有必须保持非负的金币账户，因此早期成交不需要预先注入货物或金币。

## 价格曲线

令：

- `q` 为市场净库存。玩家卖出后 `q` 增加，玩家买入后 `q` 减少。
- `P0` 为物品配置中的 `base_price`。
- `V` 为全局市场深度 `market_depth_value`。
- `K` 为全局价格倍率 `market_depth_price_multiplier`。
- `D = V / P0` 为这种物品的数量深度。

即时单价为：

```text
p(q) = P0 × K^(-q / D)
```

所以：

- 初始 `q = 0` 时，单价正好是 `P0`。
- 玩家累计净买入 `D` 件后，单价变为 `K × P0`。
- 玩家累计净卖出 `D` 件后，单价变为 `P0 / K`。

`D = V / P0` 让不同物品拥有接近相同的“金币深度”。每种物品只需要配置 `base_price`；高价物品用较少数量就能产生明显价格变化，低价物品则需要较多数量。

## 一笔交易的价格

大额交易会沿价格曲线移动，因此总价不是“当前单价 × 数量”。代码按曲线积分结算。

玩家买入 `x` 件，库存从 `q` 变为 `q-x`：

```text
买入总价 = P0 × D / ln(K)
           × (K^(-(q-x)/D) - K^(-q/D))
```

玩家卖出 `x` 件，库存从 `q` 变为 `q+x`：

```text
卖出毛收入 = P0 × D / ln(K)
             × (K^(-q/D) - K^(-(q+x)/D))
```

买入总价向上取整，防止拆单利用小数舍入少付金币。

卖出毛收入向下取整，再扣除 `market_sell_fee_rate`。默认手续费为 1%。不足 1 金币的手续费取 0，避免价值 1 金币的小额成交被全部吃掉。

## 为什么允许负库存

如果零库存意味着禁止购买，新市场只能等玩家先卖货，开服初期很容易完全没有流动性。

允许负库存后，做市商可以先卖货给玩家。库存越负，后续买价越高，也会吸引其他玩家把货物卖回来；卖货会把库存推回零并压低价格。曲线本身承担库存调节，不需要人为补货。

这种模型不保证做市商每一刻盈利。买卖取整和手续费会略微改善长期收支，但本场景允许做市商小幅亏损及负债，因此不维护市场现金池。

## 配置位置

市场参数与可交易物品位于 `config/market.lua`：

```lua
M.market_depth_value = 1000000
M.market_depth_price_multiplier = 1000
M.market_sell_fee_rate = 0.01

M.market_items = {
    {name = 'iron-ore', base_price = 100, group = 'raw'},
}
```

调大 `market_depth_value` 会减小相同交易量造成的价格波动。

调大 `market_depth_price_multiplier` 会让跨过一个数量深度后的价格变化更剧烈。

修改曲线含义时应同时增加 `market_curve_version`。场景会为五个国家重新建立净库存为零的市场；这会清除旧市场头寸，但不会修改玩家金币和背包。
