# 阶段 1 游戏内验证

## 单人

1. 载入通过阶段 0 的存档并重新加载场景脚本。
2. 确认顶部只有一个金币图标按钮，不再显示长文字和余额。
3. 点击图标，确认概览显示信用、可领取低保、进度条、领取按钮和当前表面。
4. 等待 10 秒，确认可领取数量约为 10，信用余额没有自动变化。
5. 点击领取，确认余额增加相应数量，进度条回到接近零。
6. 按 Esc 或点击关闭按钮，确认窗口关闭；重复开关不会生成多个窗口。
7. 保存并重开，确认余额及尚未领取的累计时间都保留。

## 离线累计

1. 记录一名玩家的可领取数量并退出服务器。
2. 保持服务器运行至少一分钟。
3. 玩家重新加入并打开概览，确认离线期间仍按每秒 1 信用增加。
4. 通过测试存档或临时推进游戏时间验证累计数量最高停在 `config.lua` 配置的容量。
5. 容量已满后领取，确认超出 30 小时的时间不会结转到下一次领取。

## 多人和性能

1. 两名玩家分别打开概览，确认各自进度独立。
2. 一人领取时，另一人的可领取数量和余额不受影响。
3. 所有玩家关闭概览后，UBI 不应产生周期性交易流水；只有点击领取才新增一条 `ubi-claim` 流水。

## 诊断

1. 再次执行 `/un-stage0 status`，确认阶段 0 状态仍为 `passed`。
2. 检查 `factorio-current.log`，不应出现 `[un] event`、`[un] scheduler` 或 `balance handler failed`。
